from setuptools import setup

setup(
    name='flask-file-fixtures',
    version='0.1',
    packages=['flask_file_fixtures'],  # Replace with your actual package name
    install_requires=[
        # No dependencies
    ],
)
